{% extends 'layout/layout.html.twig' %}

{% block title %}CallcenterBundle:CostCenter:create{% endblock %}

{% block body %}

    <div class="page-header">
        <h2>Expense management</h2>           
    </div>
    <div class="row">
        <div class="col-md-6 ">     
            <div class="panel panel-default">
                <div class='panel-heading'> Create Cost Center</div>
                <div class="panel-body">
                    {{ form_start(form) }}
                    <div class="form-group">
                        {{ form_row(form.code) }}
                    </div>    
                    <div class="form-group">
                        {{ form_row(form.name) }}
                    </div>                      
                    <button type="submit" class="btn btn-primary">Save</button>
                    {{ form_end(form) }}
                </div>
            </div>            
        </div>
        <div class="col-md-6 ">       
            <h5>Centers</h5>
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th> code</th>
                        <th> name </th>

                    </tr>
                </thead>
                <tbody>
                    {% for center in centers %}
                        <tr>
                            <td>{{center.code}}</td>
                            <td>{{center.name}}</td>
                            <td>
                                <div class="btn-group">
                                        <button class="btn btn-primary dropdown-toggle" href="#" data-toggle="dropdown">
                                        <span class='glyphicon glyphicon-plus'></span> <span class="caret"></span>
                                        </button>
                                        <ul class="dropdown-menu stay-open pull-right" role="menu" style="padding: 15px; min-width: 300px;">                                          
                                        <li><a href="{{ path('costcenter_delete', {'id': center.id}) }}">Delete</a></li>  
                                        </ul>
                                </div>
                            </td>                            
                        </tr>
                    {%endfor%}    
                </tbody>        
            </table>   
        </div>                 
    </div>
{% endblock %}
